# Configs

- `config.example.yml`: portable starter profile with placeholder paths.
- `config_local.yml`: local workstation profile.
- `config_hpc.yml`: cluster-oriented profile and the default fallback config.
- Each shipped profile is fully commented inline; prefer reading the YAML file
  itself for the most precise explanation of each setting and override path.
